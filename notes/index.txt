INDEX  --  Bird's-eye view of every topic and its parts.

RULE  --  Each item's one-line description is that file's CONTRACT. List the concrete sub-topics the file covers (concepts, keywords, syntax pieces) as specifically as possible, so no other file in this index accidentally repeats the same ground. If a sub-topic is already listed against one item, do NOT list it against another  --  the earlier owner keeps it.

Example (good  --  specific, claims its ground):
   6   Lists: create, add ( append , insert ), remove ( pop , remove , del ), indexing, slicing, len(), sorting

Example (bad  --  too vague, invites duplicates in later files):
   6   Lists and how to use them

1_terminal  --  Computer & terminal basics

Terminal has 4 small parts:

   1   File system: file vs folder, file name and extension (optional on Linux), tree structure, root ( / ), common top-level folders ( /bin , /etc , /home , /usr , /var , /tmp ), home folder, path (absolute vs relative), name vs path, current working directory, special symbols ( . , .. , ~ , / )
   2   Terminal: GUI vs CLI, terminal window (cursor, prompt, command, output), terminal vs shell, shell mechanics (line-to-program flow, kernel, OS basics, UNIX/Linux history, scripts, common shells — sh/bash/zsh/dash/fish), prompt anatomy ( $ vs # , ~ , variations), why use terminal (speed, precision, automation, SSH, servers, universal commands, small footprint), when a GUI is still the better tool
   3   Core commands: command anatomy (command / options / arguments, short vs long flags, combining short options), pwd, ls (hidden files  -a , long format  -l , human sizes  -h , reading  ls -l  output), cd (absolute/relative paths, shortcuts  ~ , .. , - , common errors), mkdir ( -p  for nested), touch (create empty file / update timestamp), cat (view file, multi-file concat, common errors including binary files), cp ( -r  for folders, -i  interactive), mv (also renames, -i ), rm ( -r  recursive, -i  interactive, -f  force, safety habits)
   4   Output redirection and pipes: echo (print text, spaces and word-splitting, single vs double quotes), standard output and standard error, > (send output into a file, overwrites), >> (append to a file), 2> (capture errors), redirect gotchas (empty file on failure, same file in and out, stray > ), commands that read input ( head , tail with  -f , wc with  -l / -w / -c , sort with  -r / -n , less and its keys), | (pipe one command's output into the next), chaining pipes, pipe vs redirect

2_python  --  Python from zero

Python has 34 small parts:

   1   What Python is; the  .py  file extension; Python 2 vs 3; check install ( python3 --version , -V ); install via package manager ( apt / dnf / pacman / zypper ) and sudo; python vs python3 gotcha
   2   First program: print( ... ) as function name + parens (call) + string arg; two ways to create hello.py ( echo + > redirect , text editor ); run with python3 hello.py ; python3 as interpreter; small variations (numbers, multi-line print); four common errors (file not found, missing quotes → NameError, mismatched quotes → SyntaxError, python3 not found); comments ( # whole-line and inline, no block-comment marker, # inside a string is not a comment); input() (with and without a prompt, always returns a string, wrap in int() for numbers); print() with  end=  and  sep=
   3   Variables and naming rules: named slots; assignment operator ( = ); create vs update; reading/using; compound reassign ( count = count + 1 ); hard naming rules (allowed chars, no digit start, case-sensitive, reserved-word list); style conventions ( snake_case , descriptive names, i/j/k counters); multiple assignment ( x, y = 10, 20 ); common errors (NameError undefined/wrong-case, SyntaxError digit-start/reserved-word/space)
   4   Data types: type concept and scalar vs collection; four scalars ( int , float , str , bool ); int arithmetic ( + , - , * , / , // , % , ** ) and digit-separator underscores ( 1_000_000 ); float rules ( / always returns float, mixing int+float ); bool values ( True , False ) and quick == vs = distinction; type() built-in and its <class ...> output; conversions ( int() , float() , str() , bool() ) and their behaviors (drop fractional, invalid literal); round() (decimals argument, returns a float, trailing zeros drop on print); common errors (TypeError from str+int, ValueError from bad int() input)
   5   Strings: creation (single, double, triple quotes), escape sequences ( \n , \t , \\ , \" , \' ), len(), indexing (0-based, negative), slicing ( s[start:stop:step] , [::-1] reverse), concatenation ( + ) and repetition ( * ), membership ( in , not in , case-sensitive), methods ( upper / lower / title , strip / lstrip / rstrip , replace , split , startswith / endswith , count , find , chaining), immutability (methods return new string, item-assignment TypeError, rebuild pattern), f-strings ( f"{var}" , expressions in braces, the  :.2f  format spec and how it differs from round() ), common errors (IndexError, missing  f  prefix)
   6   Lists: creation ( [ ] , nested), len(), indexing (single-level and nested), slicing (list-specific: slice-assignment on mutable list), mutability (assign to index, slice assignment shrinks/grows list), adding ( append , insert , extend , + concat-to-new), removing ( remove , pop , del , clear ), membership ( in , not in ), finding ( index , count ), sorting ( sort in-place, sorted new, reverse , sorted vs sort in-place-vs-new pattern), built-ins that take a list ( min , max , sum ), for-loop iteration (brief intro, full in section 11), common errors (IndexError out of range, ValueError from remove of missing value)
   7   Dictionaries: key-value pairs (unique keys, mutable, insertion order), creation ( { }  and the  dict()  constructor), reading ( d[key] , KeyError,  d.get()  with a default), adding and updating ( d[key] = value , update() ), removing ( del ,  pop()  with a default,  clear() ), membership ( in  checks KEYS), iterating ( for key in d ,  keys() ,  values() ,  items() , key-value unpacking), common errors (duplicate key in a literal, unhashable list key)
   8   Tuples: creation ( ( ) , nested), one-item tuple needs a trailing comma, optional parentheses, indexing and slicing, immutability (item-assignment TypeError, rebuild pattern), operations that still work ( len , in , + , * , count , index ), unpacking ( x, y = point ) and star unpacking ( *rest ), tuple vs list (when to use which), common errors (ValueError from a wrong unpack count)
   9   Sets: unique unordered items (no indexing or slicing), creation ( { } ,  set() ,  set(list)  to deduplicate), the empty-set trap ( {}  is a dict), adding ( add ,  update ), removing ( remove ,  discard ,  pop ,  clear ), fast  in  membership (hash lookup vs list scan), set operations (union  | , intersection  & , difference  - , symmetric difference  ^ ), set vs list (when to use which), common errors (AttributeError from  {} , TypeError from indexing, KeyError from  remove , unhashable list item)
   10  Conditions: comparison operators ( == , != , < , > , <= , >= ), logical operators ( and , or , not ), if (colon and indented block), if / else , if / elif / else (first match wins), nested if vs  and , truthy and falsy values (the full falsy list), the  if collection:  shortcut, ternary expression ( a if cond else b ), common errors ( =  instead of  == , missing colon, IndentationError, TabError)
   11  Loops: for over any iterable (list, string, dict), range() ( stop ,  start/stop ,  step , descending), enumerate() for index plus item, while (condition checked first), break (early exit,  while True  pattern), continue (skip a round), nested loops (inner runs fully per outer pass, break affects the innermost only), else clause on a loop (runs only when no break), common errors (infinite while, off-by-one with range, modifying a list while looping over it, wrong indentation)
   12  Functions: def (header, colon, indented body, define vs call), parameter vs argument, multiple parameters, return (early return, None by default, returning several values as a tuple), default arguments (and the after-non-default rule), positional vs keyword arguments, *args (tuple of extras), **kwargs (dict of extras) and their order, scope (local names, reading a global, local shadowing, the global keyword and why to avoid it), docstrings ( __doc__ ), common errors (missing argument TypeError, non-default after default SyntaxError, forgetting return)
   13  Reading and writing files: what file I/O is, open() and the file object, modes ( r , w , a , x , r+ , b , t ), the with block (auto-close), reading ( f.read() whole file,  for line in f: ,  f.readline()  one at a time and its  ""  end-of-file signal,  f.readlines()  into a list), readline vs readlines vs for-loop (when to use which, memory cost), writing ( f.write() , no automatic newline,  "w"  wipes on open), appending ( "a"  mode for logs), explicit  f.close()  and why  with  is better, common errors (FileNotFoundError, IsADirectoryError, UnicodeDecodeError on binary, forgetting  \n )
   14  Modules and import: what a module is (every  .py  file) and what a library is,  import <module>  with dot access,  from ... import ...  (and why to avoid star imports),  import ... as ...  aliases, writing and importing your own module (file name without  .py ), the standard library tour ( math ,  os ,  sys ,  json ,  datetime ,  random ,  re ,  pathlib ) with  random  ( choice ,  randint ,  randrange ), third-party modules and  pip install , the module search order and  sys  ( version ,  platform ,  argv ,  exit ,  path ), script vs module (and the  -m  flag that runs a module as a script),  if __name__ == "__main__" , common errors (ModuleNotFoundError, circular import, shadowing a standard-library name)
   15  Errors: what an exception is, basic  try / except , bare except and why to avoid it, catching a specific type, catching several types (tuple or several  except  blocks), how matching runs (rest of  try  is skipped, first matching  except  wins, specific before general or the later clause is dead code),  except X as e , else (runs only on success), finally (always runs, for cleanup),  raise  with a message, built-in exceptions worth raising, bare  raise  to re-raise, custom exception classes (brief, full classes in section 16), anti-patterns (bare except, silent swallow, catching too broad, try block too large)
   16  Classes: class as a blueprint vs object as an instance,  class  keyword (CamelCase, colon, indented body,  pass  placeholder),  __init__  as the constructor,  self  (what it is, why it is never passed by hand), instance attributes (one set per instance), methods (first parameter  self ), creating many instances, class attributes vs instance attributes, inheritance ( class Sub(Base) , inherited  __init__ ,  super().__init__() , method override), common errors (forgetting  self  in the signature, attributes set outside  __init__ , calling  __init__  directly, missing  self.  when reading an attribute)
   17  Type hints: what a hint is and why Python does NOT enforce it at runtime, hints on variables ( name: type = value ), hints on function parameters and the  ->  return type ( None  for no return), what actually happens when the wrong type is passed (the error comes from the operator, not the hint), who does check hints (editors, mypy / pyright, and libraries like Pydantic), basic built-in types, generics ( list[int] ,  dict[str, int] ,  tuple[int, str, float] ,  set[str] ),  Optional[X]  and  X | None ,  Union[X, Y]  and  X | Y ,  Any , running  mypy , common errors (missing colon, missing  -> , using  List  without importing from  typing )
   18  Virtual environment: what a venv is and why every project gets one (version clashes, protecting the system Python), creating one ( python3 -m venv .venv ) and what the  -m  flag does, what is inside  .venv/  ( bin/ ,  lib/ ,  pyvenv.cfg ), activating on Linux ( source .venv/bin/activate ), the  (.venv)  prompt prefix and confirming with  which python3 , working inside it, deactivating (and needing to re-activate in every new terminal), deleting a venv ( rm -rf .venv ), gotchas (forgot to activate, venv tied to the Python that made it, moving the folder breaks it, other shells need their own activate script)
   19  pip and requirements.txt: what pip is and what PyPI is,  pip install  (one or several packages), pinning a version ( == ,  >= ,  < , ranges),  pip uninstall  (and  -y ),  pip list  and  --outdated ,  pip show , what a  requirements.txt  file looks like and its rules,  pip freeze > requirements.txt  (full snapshot, and why sub-dependencies are a downside),  pip install -r requirements.txt , the after-cloning workflow (venv, activate, install),  --upgrade  (a package and pip itself), common errors ( pip: command not found , permission denied and why never  sudo pip , ModuleNotFoundError from the wrong venv, a stale  requirements.txt )
   20  Project structure: what  src/  means and that it is a naming convention, not a Python rule (other names:  app/ ,  code/ ,  lib/ , or repo root), why the layout is popular (production code vs everything else), what sits inside  src/  (one or more packages) and what does not ( tests/ ,  specs/ ,  docs/ ,  PM/ , Dockerfile and Makefile at the root)
   21  Packages and the program entry point:  __init__.py  as the package marker (runs once on first import), what it can contain (empty marker vs setup code), an almost-empty example with a docstring, a working example (relative re-exports,  ENGINE_VERSION  constant,  __all__  whitelist), why re-exports hide the internal file layout, what breaks when  __init__.py  is missing,  __main__.py  as the hard-coded entry-point name,  python somefile.py  vs  python -m somepackage  (and the error when  __main__.py  is absent), why  -m  is required for the package's own dotted imports to resolve (ImportError from a direct file run)
   22  __main__.py walkthrough: reading the header of a real entry-point file top to bottom  --  the module docstring, the  from __future__ import annotations  line, the four standard-library imports and what each one does in this file ( argparse ,  asyncio ,  json ,  signal ), the relative imports, and what has and has not run by the end of the header
   23  argparse basics: the  main()  function (its name, its two jobs, why it must exist), setting vs flag, what a PARSER is,  ArgumentParser(prog=...) ,  add_argument  with  default= ,  help= ,  type=int ,  action="store_true" , the dash-to-underscore rule, built-in  -h / --help  vs flags you define, joining adjacent string literals for long help text,  parse_args()  and its step-by-step behaviour, the  argparse.Namespace  container, positional arguments (no dash, order matters, automatic validation errors), positional vs optional and mixing both on one parser
   24  argparse subcommands: what a subcommand is (using  git  as the real-world example), flags-only vs subcommand shape,  add_subparsers()  with  dest=  and  required= ,  add_parser()  per subcommand and giving each its own arguments, a worked  todo  CLI ( add ,  list ,  done ) and the Namespace each run produces, routing with an  if args.command  chain that calls named functions, two-level help output, common errors (forgetting  dest= , reading another subcommand's attribute, unknown subcommand, no subcommand given)
   25  __pycache__ folder and .pyc bytecode files: what the folder is and that Python makes it automatically, what BYTECODE is, reading a  .pyc  file name ( greetings.cpython-311.pyc ), why it exists (import speed, and the source-newer-than-cache recompile check), when it appears (only on IMPORT, never for a directly-run script), a before-and-after folder example, deleting it safely ( rm -rf __pycache__ ), common confusions (not junk, not readable, never edit inside it, many folders is normal)
   26  json module: what JSON is and why it is used (files, network, configs), JSON syntax (the six value kinds, double quotes only, lowercase  true / false / null , no trailing comma), Python <-> JSON type mapping and what does NOT map (tuple loses its type; set, class instances and datetime raise errors), the four functions ( dumps ,  loads ,  dump ,  load  and the  s  = string trick), a worked save-and-load example with  indent= , common mistakes (single quotes, trailing comma, passing a path instead of a file object, mixing load with loads, dumping a set, expecting a tuple back, hand-typing  True  in a  .json  file)
   27  relative imports: what a relative import is, absolute vs relative (trade-offs and when to use which), what each dot means ( .  same package,  ..  parent,  ...  grandparent), a worked package example ( core.py  using  .clock ,  utils/helpers.py  using  ..clock ), the rules (only inside a package, needs  __init__.py , cannot run the file directly, no space after the dots), common mistakes (running with  python3 file.py , missing  __init__.py , wrong dot count, mixing styles, dots in a plain script)
   28  lazy / local imports: what a local import is, how it differs from a top-level one (load time moves to first call, module caching), the three reasons to use one (breaking a circular import, a heavy or optional dependency, a runtime-decided import) with a worked example each, the downsides (hidden dependencies, late failure, per-call lookup cost in a hot loop), common mistakes (using one for no reason, putting one in a hot loop, relying on a name that was never imported)
   29  keyword-only arguments: what a keyword-only argument is, the bare  *  separator (marker only, never receives a value) and what falls on each side of it, keyword-only parameters with and without defaults, why we do it (readable call sites, preventing wrong-order bugs, safe API evolution), a before-and-after example, how a bare  *  differs from  *args  (and why  *args  already makes later parameters keyword-only), common mistakes (passing by position, forgetting the star, wrong call form, confusing  *  with  *args , putting the star in the wrong place)
   30  from __future__ import annotations: which Python version each hint form needs ( typing  style anywhere,  list[str]  from 3.9,  int | None  from 3.10), what the future import changes (hints stored as raw strings instead of being evaluated) and the TypeError you get without it, a runnable example, gotchas (hints become strings so runtime readers need  get_type_hints , the import is per-file, it must be the first import), common mistakes (modern syntax without the import, variable annotations, mixing styles, expecting runtime enforcement, placing the import too low)
   31  signal module: what a signal is (an OS message, not a Python thing), SIGINT / SIGTERM / SIGKILL and which ones you can catch,  signal.signal(sig, handler)  and the handler's two parameters ( signum ,  frame ),  raise SystemExit(0)  (the exception and the exit code, the Unix 0-is-success convention, checking with  echo $? , and how it relates to  sys.exit() ), a runnable Ctrl-C demo with signal numbers 2 and 15, common mistakes (catching SIGKILL, heavy work in a handler, registering off the main thread, Windows differences, mixing with asyncio instead of  loop.add_signal_handler )
   32  asyncio basics: what concurrency is (overlapping waits, not same-instant) and how it differs from parallelism, threads / threading / spawning and why asyncio avoids them, the problem async solves (I/O-bound waiting), synchronous baseline example,  async def  and the coroutine object it returns, the three helpers ( asyncio.sleep ,  asyncio.gather  with its input-order result list and timeline,  asyncio.run  as the sync-to-async bridge), the  await  keyword and its two rules, a full sync-vs-async comparison script, when to use async and when not to, common mistakes (not awaiting, await in a plain def, blocking calls inside a coroutine, expecting CPU speedup, awaiting a coroutine twice)
   33  asyncio event loop and tasks: what the event loop is (one per thread, single-threaded, cooperative),  asyncio.create_task  for background work and how it differs from  gather , when a scheduled task actually starts running (the two-step rule, and why the first line of the coroutine body decides what prints first),  asyncio.get_running_loop  (what it returns, why you would want the loop object, and its two rules), a combined  create_task  plus  gather  example, common mistakes (calling  run  twice or nested, passing a function instead of a coroutine, never awaiting a task, passing a list to  gather , blocking the loop, calling  get_running_loop  from sync code, assuming source order)
   34  asyncio.Event and loop.add_signal_handler: what an Event is (a shared two-position flag, waiting, setting, clearing) and that every waiter must share the SAME object, the constructor and four methods ( wait ,  set ,  clear ,  is_set ), a worked two-worker example, why plain  signal.signal  is unsafe inside an async program,  loop.add_signal_handler(sig, callback)  and the two callback rules (plain sync, no arguments), Linux/macOS only, the five-step graceful shutdown pattern and why  stop.set  is passed without parentheses, common mistakes (coroutine as callback, calling instead of passing, plain signal.signal, Windows, forgetting  clear() , separate Event objects, awaiting from sync code)

3_http  --  HTTP basics

HTTP has 1 small part:

   1   HTTP idea: what HTTP is (text protocol, versions), the terms it needs (browser, mobile app and "native", curl, port and common port numbers, API vs website route), the client-server model (client asks first, stateless), anatomy of a request (request line, headers, blank line, body) and of a response (status line, headers, blank line, body), methods ( GET ,  POST ,  PUT ,  PATCH ,  DELETE ,  HEAD ) and PUT vs PATCH with the partial-data danger, status codes by family (2xx, 3xx, 4xx, 5xx) each with a real scenario, headers ( Accept  vs  Content-Type , what a cache is, what a cookie is and the login flow), the body and its  Content-Type  values, trying it with curl ( -i ,  -I ,  -X ,  -H ,  -d , and the backslash line continuation), common gotchas (wrong method, 404 vs 500, missing Content-Type, case in header values)

4_fastapi  --  Web framework basics

FastAPI has 3 small parts:

   1   FastAPI hello-world route: what FastAPI is (third-party package, runs on an ASGI server, auto-generated docs), installing  fastapi  and  uvicorn , the minimum  main.py  ( FastAPI()  instance, the  @app.get("/")  DECORATOR and its three rules, the handler returning a dict that becomes JSON), running it with  uvicorn main:app --reload  and reading that argument,  127.0.0.1  / localhost and the port (and  --port ), calling the route with curl and in the browser, the free  /docs  and  /redoc  pages, stopping with  Ctrl + C , adding a second route, decorators for the other methods and the  {id}  path-parameter syntax, common errors (ModuleNotFoundError, wrong  uvicorn  argument, address already in use, reload not firing)
   2   Path params, query params, JSON body: the three places a route takes input from, path params ( {name}  in the path matching an argument, type hints that convert, automatic 422 on a bad value, multiple path params), query params (any argument not in the path, optional ones via defaults, several joined with  & ), the JSON body (declaring a Pydantic model parameter, posting it with curl), mixing all three in one route and how FastAPI decides which is which, the shape of the automatic 422 validation response, common errors (name mismatch between path and argument, missing Content-Type, body vs query confusion)
   3   Pydantic model to validate input: what Pydantic is and what FastAPI does with it, defining a model ( BaseModel , type hints, defaults make a field optional) and using it standalone (ValidationError on a missing field or a wrong type), using it as a request body and the 422 response shape, optional fields ( Optional[str]  and  str | None ), nested models, field constraints with  Field(...)  ( min_length ,  max_length ,  ge ,  le ,  regex ,  description ), converting back with  model_dump()  (and the v1  .dict()  name), common errors (forgetting  BaseModel , a field with no type hint, mixing Pydantic v1 and v2 syntax, validation running only at parse time)

5_sqlite  --  Database basics

SQLite has 2 small parts:

   1   SQLite basics: what SQLite is (file-based, no server, one file) and when not to use it, opening the  sqlite3  shell and  .quit , statement basics (semicolon, keyword case), CREATE TABLE with constraints ( INTEGER PRIMARY KEY AUTOINCREMENT ,  NOT NULL ,  UNIQUE ,  DEFAULT ), INSERT (named columns, skipping the auto id, several rows at once), SELECT ( * , named columns,  WHERE ,  AND / OR ,  ORDER BY  with  DESC ,  LIMIT ), UPDATE and DELETE with the missing-WHERE danger, SQLite column types ( INTEGER ,  REAL ,  TEXT ,  BLOB ,  NULL ) and its flexible typing, common gotchas (missing semicolon, case-sensitive string comparison, single vs double quotes, previewing with SELECT before UPDATE or DELETE)
   2   Talking to SQLite from Python: the  sqlite3  standard-library module and its  connect / cursor / execute / fetch  shape, connecting to a file (created if missing) or to  ":memory:" , what a CURSOR is, running a statement (multi-line SQL in a triple-quoted string,  CREATE TABLE IF NOT EXISTS ,  conn.commit() ,  conn.close() ), inserting with  ?  placeholders and with named  :name  placeholders, reading rows ( fetchone ,  fetchall , rows as tuples),  sqlite3.Row  as a row factory for  row["name"]  access, update and delete with placeholders (and the one-item tuple comma), the  with  block for automatic commit and rollback, common errors (no such table, database is locked, forgetting to commit, SQL injection from string concatenation)

6_auth  --  Auth and multi-user

Auth has 4 small parts:

   1   Password hashing: why plain passwords are never stored, what a HASH function is (deterministic, one-way) with a SHA-256 example, why plain hashing is not enough (rainbow tables), what a SALT is and why it goes next to the hash, why bcrypt instead of SHA-256 (deliberately slow, built-in salt, cost factor, and the  scrypt / argon2  alternatives), using bcrypt in Python ( hashpw ,  gensalt ,  checkpw , bytes vs str, adjusting rounds), the  password_hash  column in the users table, a full signup and login flow (and why the error message must not say WHICH part was wrong),  passlib  as a wrapper, common errors (hashing a str, storing the salt separately, comparing with  ==  instead of  checkpw )
   2   JWT and cookies: how they work together
   3   Login route and session cookie
   4   "Active project" concept for multi-tenant apps

7_home_automation  --  the industry and how it solves the smart-device problem

Home automation has 3 small parts:

   1  The problem: the six terms the topic needs (bus, switch, dimmer, router, gateway, Ethernet), what a HUB / BRIDGE is and the IP address idea, a KNX IP gateway as a worked translation example, why every brand ships its own stack (protocol, hub, app, cloud account) and how KNX differs as an open protocol rather than a company, the end-user pain (many apps, no cross-brand scene, partial voice coverage, broken state sync, cloud outage)
   2  The solution: one central controller  --  protocol translation via per-brand DRIVERS and the CORE ENGINE that routes to them, a unified data model, one app for every brand, state management, scenes across brands, local-first control that survives an internet outage, and the installer dashboard that produces the signed configuration file the box loads
   3  Who builds this in the market: the four segments  --  DIY open-source, big-tech consumer, premium commercial integrators, and the professional-integrator segment where Vyom sits  --  and what separates them

Future topics  --  rough plan; folder numbers assigned when we start them

Note: the prose format below is intentional. Each topic will be converted to a numbered list, with a proper per-part contract, when its folder is created.

  html
     What HTML is; how a browser reads it. Tags, attributes, elements. Common tags (div, span, h1-h6, p, a, img, button, input, form, ul/ol/li, table). id / class attributes. Semantic tags (header, nav, section, footer). Nesting rules and document structure.

  css
     What CSS is; how to attach it (inline, internal, external). Selectors (element, class, id, combinators). Common properties (color, background, font, border). Box model. Units (px, em, rem, %). Display (block, inline, inline-block, none). Flexbox in depth. Grid basics. Positioning (static, relative, absolute, fixed).

  javascript
     What JS is; where it runs. Variables (let, const). Data types. Operators and template literals. Conditions. Loops. Functions (regular and arrow). Array methods (map, filter, reduce, find, forEach, some, every). Objects and destructuring. Spread / rest. Promises. async / await. Modules (import / export). fetch API. JSON.parse and JSON.stringify.

  node
     What Node.js is (JS outside the browser). npm and package.json. Installing packages (dependencies vs devDependencies). npm scripts (dev, build, start).

  react
     What React is and why we use it. JSX. Function components. Props. useState. useEffect. Event handlers. Conditional rendering. List rendering with map and keys. Forms and controlled inputs. Callbacks. Component composition and folder structure.

  frontend_pieces
     Vite (dev server, production build). Material-UI (components, theme, sx prop). react-markdown and GFM. fetch with credentials and cookies. SSE (Server-Sent Events) streaming. Custom hooks. Polling pattern.

  polish
     Error handling and edge cases. Small UX fixes.
